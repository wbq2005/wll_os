diff --git a/os/src/fs/block_dev.rs b/os/src/fs/block_dev.rs
index b9a6fbc..c0d0841 100644
--- a/os/src/fs/block_dev.rs
+++ b/os/src/fs/block_dev.rs
@@ -16,6 +16,7 @@ pub const DEV_LOOP_CONTROL_MINOR: u32 = 237;
 pub const LOOP_DEVICE_COUNT: usize = 4;
~
 const SECTOR_SIZE: usize = 512;
~
 const BLOCK_CACHE_LIMIT: usize = 32 * 1024;
~
+const BLOCK_CACHE_READ_AHEAD_BLOCKS: usize = 16;
~
 const ROOT_DISK: &str = "/dev/vda";
~
 const LOOP_CONTROL: &str = "/dev/loop-control";
~
 
~
@@ -169,13 +170,27 @@ impl BlockRange {
         if let Some(data) = self.cache.lock().get(offset) {
~
             return data;
~
         }
~
         let 
+available_blocks = self
~
+            .len
~
+            .and_then(|len| len.checked_sub(offset))
~
+            .map(|remaining| remaining / EXT4_BLOCK_SIZE)
~
+            .unwrap_or(1)
~
+            .max(1);
~
+        let block_count = available_blocks.min(BLOCK_CACHE_READ_AHEAD_BLOCKS);
~
+        let
  mut data = vec![0u8; 
+block_count *
  EXT4_BLOCK_SIZE];
~
         if self.read_raw_at(offset, &mut data).is_err() {
~
             log::warn!("[block] read_offset failed @{:#x}", offset);
~
             return 
-data;
+vec![0u8; EXT4_BLOCK_SIZE];
~
+        }
~
+        let first = data[..EXT4_BLOCK_SIZE].to_vec();
~
+        let mut cache = self.cache.lock();
~
+        for (index, block) in data.chunks_exact(EXT4_BLOCK_SIZE).enumerate() {
~
+            let Some(block_offset) = offset.checked_add(index * EXT4_BLOCK_SIZE) else {
~
+                break;
~
+            };
~
+            cache.insert(block_offset, block.to_vec());
~
         }
~
         
-self.cache.lock().insert(offset, data.clone());
~
-        data
+first
~
     }
~
 
~
     fn read_cached_blocks(&self, offsets: &[usize]) -> Result<Vec<Vec<u8>>, SysErrNo> {
~
diff --git a/os/src/syscall/process.rs b/os/src/syscall/process.rs
index 013b51c..0077751 100644
--- a/os/src/syscall/process.rs
+++ b/os/src/syscall/process.rs
@@ -879,7 +879,13 @@ fn sys_wait4_thread_group(pid: isize, status: *mut i32, options: usize) -> Sysca
         if !has_matching_child(&task, target) {
~
             return Err(SysErrNo::ECHILD);
~
         }
~
         
+// Register the waiter before the final child-state check.  A child can
~
+        // exit between the checks above and a bare sleep, which would otherwise
~
+        // lose its SIGCHLD wakeup and leave the parent blocked forever.
~
         match 
-crate::task::wait_queue::sleep_on_child_exit()
+crate::task::wait_queue::sleep_on_child_exit_if(|| {
~
+            Ok(peek_zombie_child(&task, target).is_none()
~
+                && has_matching_child(&task, target))
~
+        })
  {
~
             Ok(_) => {}
~
             Err(SysErrNo::EINTR) => {
~
                 if let Some((cpid, exit_code)) = reap_zombie_child(&task, target) {
~
@@ -1057,7 +1063,23 @@ pub fn sys_waitid(idtype: usize, id: usize, infop: usize, options: usize, _rusag
         if !has_matching_child(&task, target) {
~
             return Err(SysErrNo::ECHILD);
~
         }
~
         
+// As with wait4, the final event check must happen only after the
~
+        // waiter is visible to a concurrent child exit.  Keep this check
~
+        // non-consuming: the next loop iteration performs the actual reap or
~
+        // event consumption and writes the user-visible siginfo.
~
         match 
-crate::task::wait_queue::sleep_on_child_exit()
+crate::task::wait_queue::sleep_on_child_exit_if(|| {
~
+            let event_ready = (want_stopped || want_continued)
~
+                && take_child_wait_event(
~
+                    &task,
~
+                    target,
~
+                    want_stopped,
~
+                    want_continued,
~
+                    false,
~
+                )
~
+                .is_some();
~
+            let exit_ready = want_exited && peek_zombie_child(&task, target).is_some();
~
+            Ok(!event_ready && !exit_ready && has_matching_child(&task, target))
~
+        })
  {
~
             Ok(_) | Err(SysErrNo::EINTR) => continue,
~
             Err(err) => return Err(err),
~
         }
~
diff --git a/os/src/task/wait_queue.rs b/os/src/task/wait_queue.rs
index a503a0e..87e4341 100644
--- a/os/src/task/wait_queue.rs
+++ b/os/src/task/wait_queue.rs
@@ -394,6 +394,13 @@ pub fn sleep_on_child_exit() -> Result<WaitOutcome, SysErrNo> {
     CHILD_WAIT_QUEUE.sleep()
~
 }
~
 
~
+pub fn sleep_on_child_exit_if<F>(should_sleep: F) -> Result<WaitOutcome, SysErrNo>
~
+where
~
+    F: FnOnce() -> Result<bool, SysErrNo>,
~
+{
~
+    CHILD_WAIT_QUEUE.sleep_until_if(None, should_sleep)
~
+}
~
~
 pub fn wake_child_waiters() -> usize {
~
     CHILD_WAIT_QUEUE.wake_all()
~
 }
~
